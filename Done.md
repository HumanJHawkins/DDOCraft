# Done

- Duplicate-effect handling: selecting an effect already selected elsewhere no longer blocks the click — both instances render with a warning state instead. Button states: selected, duplicate warning, discouraged-but-clickable (occupied slot / taken elsewhere), filter-recommended.
- Client-side render/state architecture rewrite: catalog (category → item → slot → color → candidates) built once at load, separate from a selections store (positional + inherent), replacing the old flat-array/boundary-flag model. Save format bumped to v2.0 — **not backward-compatible** with older save files.
- Named/custom item feature: replaced the earlier plan to hand-curate a handful of named items with data. Instead, any category can be switched to "Custom," which provides:
  - Real augment slots (same color-eligibility rules as Cannith augments), added via a "+ Add Augment" control, capped at 7 per item, individually removable.
  - A flat, alphabetical inherent-effects picker covering every known effect, deliberately unfiltered by category and with no level gating.
  - A per-category overlap warning banner for a custom item's own selections, instead of the individual per-button duplicate highlight Cannith picks get.
  - Switching a category's mode never deletes its data — toggling back and forth preserves whatever was entered.
- Removed the old Cannith item-choice dropdown from category headers (dead code since the custom-item pivot — there was never more than one real option).
- Data pipeline: `source_data/equipDDO.sqlite` is the source of truth; `ddocraft.json` is generated from it via `build_db.py` → `apply_corrections.py` → `export_ddocraft_json.py`. **Never hand-edit `ddocraft.json` directly** — edit the SQLite data and regenerate.
- Corrected several bonus-type data-entry errors in the recovered source data: effect-name leakage into the bonus-type column (Blindness Immunity, Regeneration, the material-bypass family, plus two rows already using a literal "Unknown" placeholder — all normalized to "Untyped"), and a `Competance`/`Competancy` spelling split merged to `Competence`. Vitality was checked and confirmed to be a real, distinct bonus type — left as-is.
- Stood up a MariaDB backend on a dedicated dev/QA VM. Populated: `bonusType`, `augmentColor` (reference tables), `effect` (283 rows, via `db/populate_effect.py`), `effectBonusByLevel` (7,359 rows, via `db/populate_effect_bonus.py`), `itemCategory` (18 rows, via `db/populate_item_category.py`), `augmentOption` (104 rows) and `cannithCategoryOption` (1,307 rows) (both via `db/populate_category_options.py`). All migrated from the recovered `equipDDO.sqlite` data; each script's docstring documents the mapping logic.
- The one-off "Tourney Armor" stand-in item used during early named-item design has been fully removed from the data pipeline — it was never itself important, just an example.
- Built and deployed a real API server (`server/`, Express + TypeScript + `mysql2`, using the `ddocraft_web` account): `/api/health`, `/api/bonus-types`, `/api/augment-colors`, `/api/effects`, `/api/effects/:id/bonus`. Running on the dev VM on port 3001, verified against live data. Not yet wired to the client, and doesn't yet have endpoints for `itemCategory`/`augmentOption`/`cannithCategoryOption` (see To Do).
- Added table-prefixed sort-order columns, following the recovered data's `<table>SortOrder` convention: `effect.sortOrder` → `effectSortOrder`; new `itemCategorySortOrder`, `augmentOptionSortOrder`, `cannithCategoryOptionSortOrder`. The recovered `itemOptionSortOrder` turned out to be a coarse constant for the category/augment-option data (not a real per-row order) - the real curated order was recovered from `itemOptionID`'s insertion sequence instead.
- Deployed the (still old, static-`ddocraft.json`-based) client to the dev VM as its own nginx site, separate from the existing codehawkins.com config - port 8080, plain HTTP, PHP 7.4-FPM passthrough, served from `/home/claude/ddocraft`. Config at `/etc/nginx/sites-available/ddocraft`. Reachable from the LAN at `http://192.168.1.153:8080/ddocraft.php` - useful for visually testing the current app, but it's still the pre-API static build, not the rewritten client.
- Round of fixes from browser-testing the deployed dev site (2026-07-28):
  - Renamed the named-item-only "Damage Reduction (Adamantine)" effect to just "Damage Reduction" - Adamantine is the delivery mechanism, not part of the effect's identity.
  - Added weapon material-bypass effects Flametouched Iron and Crystal (Red-augment options, same family as Adamantine (Weapon)/Byeshk/Cold Iron/Silver), and Transmuting (bypasses any/all DR - material, alignment, or otherwise; Cannith-only on weapon Suffix, same treatment as the universal "Aligned" effect). Min levels are placeholder/TBD (see Known Issues).
  - Reversed the "questionable" exclusion on the 6 Insightful Ability effects - confirmed via the original hand-curated flat file that this wasn't data loss (Belt already offered Insightful Ability (Constitution)/(Strength) in the pre-migration data too), just an over-cautious Phase 1 exclusion.
  - `enchSupercededBy` (the "this effect is superseded by a better one" mechanism) is now checked globally instead of only within the same item, since the superior/inferior pair are often on different items entirely (e.g. True Sight on Goggles superseding Blindness Immunity elsewhere). Added the Blindness Immunity → True Sight link as the first real cross-item example, alongside the pre-existing Chaos/Evil/Good/Law Aligned → Aligned links.
  - Named/custom items: added a collapse toggle to augment-slot color sub-headers (Cannith slots already had one), and a freeform Description field.
  - Renamed the "Named or Custom Item" checkbox label to "Named/Custom"; moved the "+ Add Augment" control from the category header into the item's Name row (separated from the name field); removed the "e.g. Tourney Armor" placeholder text; the result table now shows "`<Item Name>` (Category)" for custom items instead of the raw `custom:Category` key.
- Collapse redesigned into a universal pruning behavior, everywhere (Cannith and named/custom alike), replacing both the old simple show/hide collapse AND the short-lived "Mark Done" toggle (superseded before shipping - same goal, one mechanism instead of two): collapsing any container - category, slot, or augment color - keeps only what's actually selected inside it and hides the rest; a container with nothing selected at all still keeps its own header visible as a button to re-expand, but empty descendants nested inside a collapsed ancestor vanish completely rather than showing as empty placeholders. Also fixes a real bug: named-item augment slots stopped collapsing at all once the augment "remove" (×) button was added, because the click handler had silently shrunk to just the label text instead of the whole header cell.
- Empty slots (Cannith, named/custom augment slots, and Inherent Effects) no longer vanish when collapsed - they show what's still available instead: "N unused options available." (or "1 unused option available.", or "No unused options available."). The count only includes genuinely good picks - level-gated in, passing the active filter, AND not already flagged as a discouraged duplicate elsewhere (same effect type, or whatever it supersedes, already selected on another item) - not merely whatever would render as a clickable button at all. Inherent Effects uses its own wording, "No effects identified.", with no count - those aren't really options to choose so much as properties to identify.
- Phase 1 of DB-backed character saves: added a `characterBuild` table (`db/ddocraft_schema.sql`) and a full CRUD API (`server/src/routes/characterBuilds.ts`, mounted at `/api/character-builds`) - create, list (light-weight, no build payload), fetch one (full payload), update, delete. The save-file payload itself (positional/inherent selections, categoryMode, customItems, collapsed state) is stored as one opaque `buildData` JSON column rather than normalized into rows, so client-side save-format changes never require a server migration; `charName`/`charLevel`/`description` are pulled out as real columns for a future list view. `userId` is a caller-supplied placeholder for now (`ddocraft_web` already had the needed grants on `ddocraft.*`, so no new GRANT was required) - real identity arrives in phase 2 once GateIron.com's shared account system (site-wide accounts via Auth.js, MySQL-backed sessions, Google/GitHub OAuth - a decision made alongside this work but implemented at the GateIron.com level, not in this repo) exists to authenticate the caller. The client isn't wired to this API yet (see TO DO.md #5) - local JSON file save/load stays the only way to save for now, and isn't being retired by this.
- Added a temporary test harness (`ddocraft.php`/`ddocraft.js`) so the DB-backed save/open API can be exercised from the actual browser, not just curl - a "Test User ID" field plus Save to Server / Open from Server buttons, explicitly throwaway (replaced wholesale once real accounts exist). Also added an nginx `/api/` proxy on the dev site so the browser can call the API same-origin with no CORS setup, and fixed a real bug this surfaced: `charLevel` is a string straight from the HTML input's `.value`, which the client's own loose comparisons tolerate but the API's strict integer check didn't - now coerced before posting.
- `characterBuild.characterBuildId` is now an app-generated random GUID (v4) instead of a sequential auto-increment integer, replacing the id entirely rather than adding a second identifier alongside it. This is what makes "open a build" a safe link to hand to someone else: a random 122-bit value can't be guessed or enumerated the way a small sequential integer could, so there's no need for a separate "share" mechanism, permission toggle, or token table - opening your own build and opening a build someone sent you are the same GET request, and knowing the id is itself sufficient (same principle as a "anyone with the link" Google Docs URL). The single-build GET route dropped its `userId` requirement accordingly; list/update/delete still require the owning `userId`.
- Added `buildChecksum`: a SHA-256 hash over a canonicalized subset of a build - `charLevel` plus every item/category field (selections, categoryMode, custom item names/descriptions/augment colors) - deliberately excluding ownership/history (id, userId, charName, description, dates) and UI-only state (collapsed, and the arbitrary per-augment-slot add-order id). Canonicalization sorts everything first, so the same set of choices hashes identically regardless of what order they were made in - verified directly: two builds built in reverse order, with different owners, names, and internal augment ids, produced the identical checksum, while changing `charLevel` alone changed it. Lets two saves be recognized as mechanically the same build regardless of who made them or what they're called - e.g. surfacing builds independently arrived at by multiple users (see TO DO.md's "popular named/custom items" idea, which this now directly feeds) or catching a redundant re-save.
- `buildChecksum` is now actually used: saving to the server checks for an existing row with the same `userId` + `charName` + `buildChecksum` first. A match updates that row in place (still persisting `description`/`appVersion` in case either changed even though the build/name didn't) and reports `created:false` with the existing id, instead of piling up duplicate rows every time someone re-saves without meaningful changes. `charName` deliberately isn't folded into the stored checksum itself (which stays name-agnostic, for the cross-user matching use case above) - it's composed as a separate condition on this one query instead, since a mechanically-identical build saved under a different name is a deliberate second save, not a duplicate of the first.
- Replaced the prompt()-based build picker with a real "Open Build" modal (same show/hide pattern as Preferences/Help/About): a sortable table (Name/Level/Updated, click a header to sort/reverse) with formatted local datetimes. Each row's Open control is a real `<a href="ddocraft.php?openBuild=<guid>">` rather than a JS-only button, specifically so the browser's native right-click "open in new tab," middle-click, and ctrl-click all work unmodified - a plain click is the only case intercepted, for a fast in-page load. A new `loadCharacterBuildFromUrl()` check on page load means a direct navigation to that URL (new tab, bookmark, pasted link) loads the build on its own, the same as going through the dialog.
- Save and Open moved to the top icon row (matching Preferences/Help/About's existing style and show/hide pattern) and now point at the server-backed save/open built above - that's the real save mechanism now, not the old downloadable file. New hand-drawn flat SVG icons (`image/save.svg`, `open.svg`, `download.svg`) match the existing PNG set's look: solid black silhouette, transparent background, no shading - drawn directly rather than via an image-generation model, since that style is exact geometric glyph work rather than illustration. Also added Download: a new, distinct action producing a compact Markdown summary of the build grouped by item - explicitly download-to-read, not a save format (no relation to the old local-file download). First pass only; full layout/format (Markdown vs. PDF, visual polish) is still an open design question. The old bottom bar (local file Save/Open, character name field, Test User ID, Save-to-Server/Open-from-Server test buttons) is untouched for now - a larger character metadata (name/description/filters/level) overhaul is coming that would make any cleanup here premature.
- That character metadata overhaul: added a compact "Character Info" section above the options UI - Character Name (moved from the bottom bar, same "Unnamed" default behavior) and Character Level (now required, 1-36, and starts genuinely empty rather than defaulting to 36), plus a collapsible "Highlight" section holding the recommendation/highlight-filter checkboxes that used to live in the Settings modal. Character level now really gates the app: with no valid level set, every category renders forced-collapsed (each category's own manual collapse state is preserved underneath, not overwritten, and comes back once a valid level is set again) and the Save/Download icons visibly grey out and stop responding to clicks. `handleFilterLevel` was renamed `handleCharLevelChange` and now validates/clamps input client-side rather than trusting whatever the number field held - an invalid entry normalizes to `""` (a real, checkable "not set" state) instead of silently keeping stale data. The Settings/Preferences modal and gear icon are gone entirely - once Highlight moved inline, there was nothing left in it, and no near-term settings to plausibly justify keeping an empty one around (its now-unused icon moved to `obsolete/`).
- Fixed a real download bug: `downloadJSON()` (used by both the old local-file save and the new Download report) could leave a blob download stuck showing as an unfinished `.crdownload` in the browser even though the file was already written completely. Appending the link to the DOM before clicking, removing it right after, and delaying the object-URL revoke avoids the race with the browser's own read of the blob.
- Added a self-referencing `characterClass` table, seeded with DDO's 15 base classes (taken from the existing forBarbarian-style filter checkboxes, not independently guessed), plus `GET /api/character-classes` and a Class dropdown in the Character Info section. `parentClassId` lets an Iconic Hero eventually be modeled as its own row pointing at its base class - not seeded yet, since the current Iconic roster needs confirming first (see TO DO.md Known Issues). Immediate use is cosmetic only, same "psychological marker" framing as the name/level fields; selection round-trips through save/open by class name, not id.
- Audited every SQL query in `server/src/routes/*.ts` for view candidates (Jeff's request, prompted by noticing there were no views in the DB at all). Almost all are single-table selects with no join or computed shape to encapsulate - a view would add nothing over the table itself. The one real case, `effect` joined to `bonusType`, is now `vw_effectDetail`, used by `GET /api/effects`. The bigger case - a view reconstructing the flat forBarbarian-style shape once those columns move into a real rating table - is scoped in TO DO.md alongside that normalization work, not created yet since there's nothing to select from until that table exists.
- Rewrote the Download report to a genuinely compact format: one title line (name, level, class), one `## Category: CustomName` header per item, a Cannith item's Prefix/Suffix/Extra collapsed into a single "X of Y with Z" sentence (matching how DDO players already describe crafted gear) instead of one bullet per slot, a custom item's inherent effects collapsed into one alphabetical comma list, and a shared "**Augments**: Color: Effect, ..." line for either kind, only when augments are actually selected. Categories with nothing selected are skipped entirely.
- Fixed a real layout bug and redesigned Character Info: it had been rendering above the page's own title row entirely (ahead of the app name and Save/Open/Download icons) - moved to sit directly under it. Replaced the always-visible form-row look with two modes sharing one toggle (Highlight's own separate collapse is gone - now there's just one, for the whole section): collapsed shows a single title-like presentation line ("Name, Level N Class" - the same format as the Download report's title), expanded shows the actual edit fields plus the highlight checkboxes. Presentation mode is unavailable without a valid level, forced back to edit mode instead, same reasoning as the options UI forcing collapse - nothing to show off yet. Edit-mode fields moved off a wide label-left/control-right row onto compact label-above-input field cards that sit close together. Added a warm gold/serif "character sheet" accent (border, labels, presentation title) instead of looking like a generic settings form. Opening a saved build now lands in presentation mode immediately, since showing off a finished character is a different moment than actively building one.
- Fixed two follow-up layout bugs found testing the above: (1) the section was still visually overlapping the title row - the title's two `<h1>`s are old-school `float:left`/`float:right` with nothing clearing them, so the next block element started at the top of that same floated area instead of below it; added `clear: both`. (2) the Class dropdown sat lower than the Name/Level inputs - a pre-existing global `select { margin: 1vw auto auto auto; }` rule (used elsewhere in the app) was giving it a large top margin the `<input>`s didn't have; overridden to `margin: 0` within the character info field cards. Also added `color-scheme: dark` on `body` so native form widgets (the level input's up/down spinner arrows, checkboxes, scrollbars) render in dark-appropriate colors instead of a barely-visible default light gray - the level field was already `type="number"` with `min`/`max` 1-36 and already stored/handled as a real integer end-to-end, it just didn't look like a number picker against the dark background.
- Fixed a third round of sizing issues: the Name field was still carrying `class="charName"` left over from the old bottom-bar markup, which set a hardcoded `width:240px; height:30px` - unused anywhere else once that old field was removed, so both the class and its now-dead CSS rule are gone; Name now sizes itself the same way Level/Class already do. Also enlarged the shared field font/padding for all three fields - a number input's native spin buttons don't shrink along with a small font, so at the previous small size they looked oversized/disproportionate relative to the field itself; a bigger field brings the proportions back in line.
- Reversed the recommendation-highlight color scale (`getHighlight()`): a light, faintly blue-tinted background at the low end diverging to a darker, more saturated blue as more checked Highlight filters match, instead of the old muted-blue-to-bright-blue scheme. The new dark/peak anchor reuses the exact blue that used to sit at the low end of the old scale (already proven legible with black text), and every visible unselected/unhandled/unblocked button now goes through this scale (not just ones scoring above the floor), so there's no more visual jump between "not specially colored" and "highlighted" - confirmed each individual `forX` category weight in the source data is already a real graduated value (0 up to 32, not boolean), and the additive per-checked-category sum in `getEnchFilterValue()` was already correct.
- **Retired the 15 per-class Highlight filters entirely** (forBarbarian, forFighter, forPaladin, forRanger, forAlchemist, forArtificer, forBard, forRogue, forMonk, forCleric, forDruid, forFavoredSoul, forSorcerer, forWarlock, forWizard) - a real design flaw surfaced by testing the reversed color scale above: rating an effect's usefulness "for Barbarian" baked in an assumption about how that class is supposed to be played (e.g. treating big melee damage as inherently more "Barbarian" than AC or a ranged build), which doesn't hold - DDO players build the same class many different ways, and the framing denigrated valid playstyles (a "Bowbarian," an AC-focused Barbarian, etc.). The five build-purpose filters (forMeleeDmg, forRangedDmg, forACDefence, forResistDefence, forHitPoints) plus allEnch/basic/nonscaling stay - picking a playstyle goal directly is the actually-useful mechanism; a class stereotype was never necessary to get there. Removed end to end: the 15 columns are gone from the live MariaDB `effect` table (`ALTER TABLE ... DROP COLUMN`) and its schema, the 15 checkboxes are gone from the Highlight section, and all related client code (`buildCatalog()`, `initFilter()`, `getEnchFilterValue()`) is gone. The raw recovered SQLite data (`source_data/equipDDO.sqlite`) still carries these columns internally - only `export_ddocraft_json.py`'s field list was trimmed, so nothing is lost even beyond git history; re-adding this (or building something better from it) later just needs the export list widened again. TO DO.md's planned "normalize into an `effectClassBenefit` table" item is removed too, since it would have been a fancier version of the same now-rejected idea, not something worth building.
- Brightened unselected-option text in slots with something already selected: `.handled`/`.blocked` button text goes from `#555` (barely legible against the `#141413` background) to `#8a8a80` - the reversed, higher-contrast recommendation scale above made the old dim text stand out as noticeably harder to read by comparison.
- Changed the `.selected` (a chosen effect) button color from green to a light orange (`#f0a030`, hover `#f7b855`) - green was clashing with the new light-to-dark-blue recommendation scale.
- Added a sense of how complete a saved build is: a new `effectCount` column on `characterBuild` (`buildData.positional.length + buildData.inherent.length`, computed server-side at save time) shows as a sortable "Effects" column in the Open Build dialog, between Level and Updated.
- Added a real overwrite step to saving: an exact content+name match still just updates in place as before, but saving genuinely different content under a name that already has an active build now soft-deletes the old row (a new `deletedDate` column, NULL = active) and inserts a new one, instead of silently piling up same-named rows forever. If the new content has 5 or more fewer selections than what it would replace, the server declines (409) with both counts instead of proceeding, and the client shows a confirmation ("Save over the existing `<name>`, Level `<N>` that has `<x>` selections made, with this version that has `<y>`?") - resubmitting only on Yes. List and single-build GET (including the GUID-based open link) both filter out soft-deleted rows, so a superseded build disappears from the Open dialog and can't be reached via a stale link either.
- Added build version history and rollback, giving the soft-delete above a real purpose beyond just tidiness. Deliberately scoped to one build's own name rather than a general "browse everything deleted" recovery list - since there's always exactly one active row per (userId, charName) by construction, a rollback is always a plain two-row swap (deactivate whatever's active now, reactivate the target), never a rename or a naming conflict to resolve. A History button next to each row's Open link in the Open Build dialog opens a new Build History modal (reuses Open Build's table/link styling) scoped to that name; the active version gets a real Open link, every other version gets a Roll Back button with a confirmation ("Are you sure you want to roll back to the version from `<date>` with `<x>` effect selections?"). Rolling back to what was just deactivated undoes a rollback the same way, with no separate undo mechanism needed - confirmed directly by rolling back and forward between two versions and checking each was openable only while active.
- Named/custom augment slot labels dropped their color parenthetical - "Augment 1 (Blue)" is back to plain "Augment 1", matching Cannith - the color already shows via the color sub-header in column 2, and repeating it made collapsed rows taller than necessary.
- Fixes from browser-testing the History/Rollback dialog: opening it now hides the Open Build dialog instead of stacking on top of it, and its close button returns to Open Build instead of leaving both hidden (a new `handleCloseBuildHistory()`); the build list's `min-height` keeps short lists from looking cramped at the bottom. Investigated the reported "no Roll Back buttons" separately - traced `renderBuildHistoryTableBody()` against a live two-version history response and confirmed the render logic and server data are both correct; the likely explanation is that the tested build only had one version ever saved (nothing yet superseded, so nothing to roll back to), not a code defect.
- Fixed a charLevel type bug: `initFilter()` and `handleLoad()` assigned `charData.saveFile.charLevel` straight from a form `.value`/loaded JSON without coercing to a number, while `hasValidCharLevel()` requires `Number.isInteger()` - a build loaded (or saved) with a string-typed level would permanently fail that check and leave Save/Download disabled, with no selection change able to fix it since only the level field itself ever touched that value. Both sites now coerce with `Number(...)`.
- Added a Delete action to the Open Build dialog. `DELETE /api/character-builds/:id` changed from a hard SQL delete to a soft delete (`deletedDate = NOW()`), consistent with overwrite/rollback. Deleting a build's only active version currently leaves it unreachable from the UI (History is only reachable from an active row), so the confirmation is upfront that there's no undo right now - a general "browse deleted/orphaned builds" recovery screen is noted as a deferred Maybe item rather than built now.
- Collapse state redesigned to be fully hierarchical and independently tracked at every level, replacing the old inherited-at-render-time "pruneMode" model: collapsing (or expanding) a category now really does collapse/expand every one of its slots and their colors, immediately, in the data - not just visually. So a category you collapsed can have any individual slot re-expanded on its own, and the category itself now stays correctly marked as collapsed only for as long as every one of its slots agrees. The reverse works the same way one level down: manually collapsing (or expanding) every color in a slot marks that whole slot collapsed/expanded too, which can in turn mark the category. Deliberately not done: expanding a single slot does not mark its category expanded - only full agreement across every slot moves the category's own flag.
- Added real unsaved-changes tracking. A `lastSavedSnapshot`/`isDirty()`/`markSaved()` trio compares current content (positional/inherent selections, categoryMode, customItems, name, level, class - deliberately not collapse state, which isn't "content") against a baseline set at the last successful save or load. Three user-facing results: (1) the save-succeeded `alert()` is gone - a successful save just quietly disables the Save button, and any further real edit re-enables it, via the same `updateSaveDownloadEnabled()` already gating on a valid level; (2) opening a different saved build, rolling back, or loading a file from disk now checks for unsaved changes first via a shared `confirmDiscardUnsavedChanges()` guard - if dirty, offers to save (kicks off a save, then the user retries the action once it finishes) or explicitly discard, before wiping the in-memory build; (3) nothing changed for the very first page load of a shared/bookmarked link, which never goes through the guard. `initialize()` takes its own baseline snapshot right before that first load check, so a completely untouched fresh page is never "dirty."
- Added a centered Done/Edit button under the "Inherent Effects" label on a custom item, toggling the same collapsed state as clicking the label itself (no separate handler - it sits inside the label cell's existing click target and relies on bubbling). The label text alone stopped reading as an obvious "click here to collapse" once a long picker list was open below it.
- Fixed a page-breaking bug from the unsaved-changes work above: `initialize()`'s baseline snapshot ran synchronously during the script's own top-level execution, but `let lastSavedSnapshot` was declared much further down the file - still in its temporal dead zone at that point. Reading it threw, which (uncaught) aborted the rest of the script's top-level execution, permanently leaving every `let` declared after that point (`openBuildList`, `buildHistoryList`, etc.) uninitialized for the page's whole life - silently breaking Open and leaving Save/Download stuck at whatever state they'd last rendered before the crash. Fixed by moving the declaration above `initialize()`'s call site. Caught and verified with a small Node/stubbed-DOM harness that actually executes the file end to end (`initialize()`, an Open click, a level change) rather than relying on `node --check`, which only validates syntax and wouldn't have caught this - worth reusing that harness for anything touching top-level state going forward.
- Fixed a real collapse bug, unrelated to the above: `renderColorGroup()`'s collapsed check was gated on `isAugment`, so Prefix/Suffix/Extra (single-color, non-augment slots) never actually respected their own collapsed state at the color-group level, even though `setSlotCollapsed()` already tracked it for them the same as any augment color. A collapsed Prefix/Suffix/Extra slot with something selected in it showed every option instead of just the selected one.
- Soft-delete and rollback status changes no longer bump `updateDate` (`updateDate = updateDate` in the same UPDATE statement suppresses `ON UPDATE CURRENT_TIMESTAMP` - verified empirically against the live DB) - they're status changes, not content changes, matching the same reasoning already applied to `deletedDate`. Also widened `characterBuild.createDate`/`updateDate` to `DATETIME(3)` (millisecond precision) - the previous whole-second `DATETIME` let two rows genuinely tie when a save/overwrite/rollback landed in the same wall-clock second as another (easy to hit, since these often happen back-to-back), and a tie doesn't sort in any guaranteed order - so History or Open could show a soft-deleted row ahead of the version that superseded it. Confirmed fixed by firing two overwrites back-to-back with no delay (previously tied at whole-second resolution) and checking they now sort correctly by a real sub-second timestamp difference.
- Open Build dialog: moved Delete to its own column on the opposite side of the table from Open/History. Both the Open Build and History dialogs now recognize the build currently loaded in the editor - its row shows no Open control if nothing's changed since (nothing to open), or relabels to "Revert" if it's been edited since (loading it again really does discard those edits) - via a new shared `renderOpenLinkCell()` and a `currentServerBuildId` tracking whatever was last loaded or saved this session.
- Preview only, not a final decision: tried the Character Info section's brown background (`#1c160e`) and light-orange borders (`#a8862d`) on every table in the app, leaving the outer page background and individual controls untouched.
- Removed the inconsistent `b0.8`/`b0.98` version strings from the browser tab title and the on-page heading - no version number in the app title.
- Removed DDOCraft's only PHP dependency: `ddocraft.php`'s entire PHP footprint was a visitor counter (`ddocraft_counter.txt`/`ddocraft_log.txt`) plus the `session_start()` it needed to dedupe a visit. Renamed the file to plain `ddocraft.html` (git history preserved via `git mv`), deleted the PHP blocks and the now-dead counter/log files, updated the two `ddocraft.php?openBuild=...` URL builders in `ddocraft.js`, and dropped the dev nginx config's `location ~ \.php$` PHP-FPM passthrough entirely (GateIron.com's other sites still run PHP-FPM; this app no longer touches it at all). No counter replacement built - real usage data belongs in the DB-backed system, not another file-based counter (see TO DO.md).
- Fixed the brown/orange table styling follow-up correctly instead of just reverting it: the color scheme itself was fine, but `.modal-content` (shared by every dialog) is a light background with dark text, so the new dark-brown table sat inside a white box with text that stayed dark - illegible. Restored the brown/orange on the main page (table/td, `.result`), and gave Open Build and History specifically (scoped by id, not touching Help/About) a matching dark background and light text, plus a close-button hover fix so the × doesn't go invisible against the new dark background.
- Added custom hover tooltips to the top icon row (Save/Open/Download/Help/About), rather than relying on the native `title` attribute - browsers don't expose control over its (long, ~1s+) delay, and a specifically fast, deliberate appearance was wanted. A `data-tooltip` attribute plus a CSS `::after` bubble with a 0.3s hover delay before fading in.
- Removed the old local-file Save/Open UI and the "temporary test harness" Server save/open block (Test User ID field, duplicate Save/Open-to-Server buttons) from the bottom of the page - both were fully superseded by the top icon row, which already calls the same `handleSaveToServer()`/`handleLoadFromServer()`. Removed the now-dead JS behind them too (`handleSave()`, the `loadFile` file-input handler, `getNameFromOldFilename()`/`getLevelFromOldFilename()`, the `.loadSave`/`.loadSaveBtn` CSS) rather than leaving orphaned code, and hardcoded `getTestUserId()` to `1` instead of reading the now-gone input - it was always a placeholder for a real authenticated session (see TO DO.md), never a genuine per-user picker.
- Renamed `getTestUserId()` to `getUserId()` - it stopped being a test-only helper the moment its visible input field was removed above; it's now just the (temporarily hardcoded) real source of `userId` for every save/open/history/rollback/delete call, same as it'll be once real accounts exist.
- Fixed two confirmation-message issues found testing the unsaved-changes guards from the previous round: (1) Revert's confirmation was routed through the generic "save first?" guard, which contradicted Revert's whole purpose (intentionally discarding changes) - it now has its own plain `confirmRevert()`: "Discard changes and revert to last saved copy?", Yes/Cancel, no save option offered at all. (2) The generic guard's "save first?" wording is now "Save changes before leaving `<name>`?", and - the real fix - choosing to save now actually completes the original action afterward instead of requiring a second click: `handleSaveToServer()`/`submitCharacterBuildSave()`/`handleSaveOverwriteConfirmation()` all thread through an optional `onSaved` callback, and `confirmDiscardUnsavedChanges()` is now callback-based (`proceedFn`) rather than returning a synchronous boolean, so opening a different build (or rolling back) while dirty and choosing to save really does open (or roll back to) the target once the save completes. Verified with a Node harness: save fires, then the target build's own load fires right after, with `isDirty()` correctly reset.
- Fixed a second, more severe instance of the temporal-dead-zone crash class first found and fixed two rounds ago: `CHARACTER_BUILD_API_BASE` (a `const`) was declared far down the file, still in its temporal dead zone whenever `initialize()`'s own synchronous execution reached it - which happens on every page load that includes a shared build's `?openBuild=<guid>` in the URL (`loadCharacterBuildFromUrl()` → `loadCharacterBuildFromServer()`, both synchronous before `fetch()` actually fires). Confirmed via the real browser console this time (`Uncaught ReferenceError: Cannot access 'CHARACTER_BUILD_API_BASE' before initialization`) - an uncaught crash there permanently breaks every `let`/`const` declared after it for the rest of the page's life, which is exactly why Open ignored clicks and Save silently never reached the server: both throw the identical error the instant they're invoked. The prior round's harness never caught this because it never tested a `?openBuild=` URL. Rather than patch this one variable, moved every remaining top-level `const`/`let` in the file (`AUGMENT_SLOT_CAP`, `AUGMENT_COMBO_COLORS`, `WEAPON_CATEGORIES`, `CHARACTER_BUILD_API_BASE`, `openBuildList`/`openBuildSortColumn`/`openBuildSortAsc`, `buildHistoryList`) up before `initialize()`'s call site, closing the whole class of bug at once. Verified with a harness reproducing the exact failing URL from the console trace - `initialize()`, an Open click, and a Save click all complete cleanly now. Directly navigating to a shared build link in a new tab (previously a listed Known Issue) is fixed by this same change, since it's the identical code path.
- Found and fixed the real cause of the missing icon tooltips (separate from the crash above, as suspected): all 5 tooltips lived on `<img>` elements via `::after`, and `<img>` is a replaced element - no browser renders `::before`/`::after` generated content on replaced elements at all, regardless of what the CSS declares, so the tooltip silently never rendered on any of them. Wrapped each icon in a plain `<span class="iconButtonWrap" data-tooltip="...">` (a real, non-replaced element) and retargeted the tooltip/position/disabled-state CSS at that wrapper instead of the image - `id="iconSave"`/`id="iconDownload"` moved to the wrappers too, with no JS changes needed since `classList.toggle("disabled", ...)` works identically on any element, and a disabled wrapper's `pointer-events: none` is inherited by the `<img>` inside it, so disabled icons still correctly suppress both clicks and their own tooltip.
- Added a `beforeunload` guard for unsaved changes - catches a refresh, closing the tab, or navigating away entirely, none of which the app's own `confirmDiscardUnsavedChanges()`/`confirmRevert()` guards can reach (those only fire on in-app actions). Only engages when `isDirty()`; can't customize the browser's prompt text or offer a "save first" option (the page unloads immediately if the user chooses to leave), but it's a real safety net for exactly what the in-app guards can't cover.
- **Retired the static `ddocraft.json`/SQLite export pipeline, completing the "direct to DB" migration (TO DO.md items 1/2) end to end** - done overnight 2026-07-30 while Jeff was away, working solo through the full plan (endpoint → verify → client swap → verify again → retire the old pipeline), stopping to fix real problems found along the way rather than plowing past them:
  - Built `GET /api/catalog`, a single bulk endpoint replacing the static export, backed by two new SQL views (`db/create_catalog_views.sql`) that reconstruct the exact flat row shape `ddocraft.json` already produced - `buildCatalog()` client-side needed zero changes as a result, only which URL `loadEnchantmentOptions()` fetches from.
  - Verified exhaustively before ever touching the client: a full row-and-field diff between the new endpoint's output and the live `ddocraft.json`, by key (category, item, slot, color, effect name) - not spot checks. First pass found real discrepancies, each run to ground rather than dismissed or silently patched around:
    - MariaDB's `effect` table had never been kept in sync with two corrections that landed in the SQLite source after the original 2026-07-28 migration - the 6 "Insightful Ability" per-stat effects (reinstated after being flagged questionable) and the 3 new weapon materials (Flametouched Iron, Crystal, Transmuting) simply didn't exist in MariaDB at all. Added both, with their augmentOption/cannithCategoryOption bindings pulled directly from the corrected SQLite source (`db/populate_insightful_ability.sql`, `db/populate_weapon_materials.sql`) - not guessed, and each stat's actual (non-uniform) category bindings checked individually rather than assumed symmetric.
    - The "Damage Reduction (Adamantine)" → "Damage Reduction" rename (a 2026-07-28 correction) had similarly never synced - fixed directly on the `effect` row (name, key, and description all updated to match the corrected source).
    - Initially misread a genuine data asymmetry as a bug: Melee1/Melee2/Ranged/Orb's Augment 1 offering fewer colors than Augment 2 looked like an old-data gap and got "fixed" to be uniform - **Jeff caught this immediately as a real DDO mechanic** (a weapon/orb always has exactly one Red-eligible augment slot, never Blue and Yellow both offered together), not an error to correct. Reverted, and encoded the exact confirmed per-slot pattern as a literal exception in the view rather than trying to derive or generalize a rule from a single example - flagged in `TO DO.md` as an open question whether it extends to the client's separate custom-item color picker, rather than guessing further at something already gotten wrong once.
    - Populated `effectEquivalencyGroup`/`effectEquivalencyMember` for the first time (`db/populate_effect_equivalency.sql`) - designed in the schema back on 2026-07-29 but never used. Reconstructs the retired flat `enchSupercededBy` column's 5 known relationships (the Alignment family, True Sight vs Blindness Immunity) from real relational data - a member's supersededBy is the name of any other same-group member with a strictly higher `qualityScore`, populated here for the first time too rather than left NULL.
    - After every fix: re-ran the full row-and-field diff from scratch. Final result: 4013/4013 rows, every field, byte-for-byte identical to `ddocraft.json`.
  - Only then rewired `loadEnchantmentOptions()` to fetch `/api/catalog` instead - and verified again, a different way, before calling it done: ran the actual deployed `ddocraft.js` bytes (not local source) through a Node/stubbed-DOM harness against the actual live endpoint response (not a fixture) - `initialize()`, a full render pass, and a spot-check of the Melee1 asymmetric-slot fix all confirmed working end to end.
  - Moved `source_data/equipDDO.sqlite`, `ddocraft.json`, and the three export-pipeline scripts (`build_db.py`, `apply_corrections.py`, `export_ddocraft_json.py`) into `obsolete/` (descriptively renamed, matching that folder's existing convention) only after all of the above passed - not deleted, and not touched before the replacement was proven. Updated the three still-relevant historical migration scripts (`db/populate_effect.py`, `populate_item_category.py`, `populate_category_options.py`) to point at the new location, since they still document how MariaDB was originally populated even though they're not meant to run again.
  - Net effect: MariaDB is now the sole source of truth for the whole catalog, end to end, with three known data gaps closed in the process that the static export had been silently carrying forward unnoticed.
- Fixed a real bug found by inspection, not by testing: `handleRename()` and the old `handleClassChange()` both updated state that's part of `isDirty()`'s snapshot (`charName`/`className`) but never triggered a re-check afterward, unlike every other field (level changes and selections always go through `renderEnchantmentOptions()`, which calls `updateSaveDownloadEnabled()`). Save's enabled state silently missed name/class edits as a result. Both now call `updateSaveDownloadEnabled()` directly.
- **Added multi-classing and an optional character description field.** `charData.saveFile.className` (a single string) became `classNames` (an array, capped at `MAX_CLASSES = 3` - DDO's own multi-classing limit), stored by class *name* rather than `characterClassId`, matching the same round-trip-robustness reasoning the old single field already used. Old saves with just `className` still load fine, wrapped as a one-element array. The picker reuses the Inherent Effects Done/Edit pattern (a collapsed summary like "Fighter/Rogue/Barbarian" or "(none)", click to expand into a button grid, click to toggle, Done to collapse) rather than the old `<select>` - `allCharacterClasses` is now fetched once into a plain array instead of populating a dropdown, and unselected buttons past the cap get the existing dimmed `.handled` treatment. Description is a new optional textarea, deliberately excluded from `computeBuildChecksum()` (confirmed and left that way per Jeff - the checksum captures the equipment/effect choices, not character metadata like name/class/description, which is what makes it useful as a "same underlying build" signal across different names) - `handleSaveToServer()` now sends the real value; the server's top-level `description` column already existed and worked end to end, only the client-side field was missing. Verified with a harness against real live `/api/catalog`/`/api/character-classes` data: 15 classes load, the 3-class cap is enforced (a 4th click no-ops rather than erroring or bumping an existing pick), deselection works, description dirty-tracking flips correctly, and both the presentation title and the Markdown report join `classNames` with "/".
- **Built `test/harness/` - a reusable fake-browser test environment, replacing the one-off `/tmp` scripts every prior feature above was verified with.** `dom-stub.js` reads the real `ddocraft.html` to build a `REAL_IDS` set (so `getElementById()` faithfully returns `null` for stale references, same as a real browser), then runs `ddocraft.js` via indirect `eval` - since that executes in the true Node global scope, top-level `function` declarations become callable globals afterward (`page.global.someFunction(...)`), while top-level `let`/`const` (like `charData`) need an explicit `exposeSrc` line (`global.__c = charData;`) appended before the eval to be reachable - this mirrors exactly how the earlier `/tmp` harnesses worked, just packaged for reuse instead of rewritten each time. Includes a synchronous-XHR stub matching the app's own `async=false` pattern, with routes either served from a local fixture or shelled out to `curl` against a real endpoint for true end-to-end checks. `test/harness/example.js` is a self-test/usage demo; `test/harness/named-item.js` (below) is the first real regression test built on it. Verified working both offline (fixture routes) and against the live deployed server.
- **Added Named Item min-level and a per-user reusable Named Item library.** Two parts, both requested and built together:
  - *Min level*: `customItems[category]` gained a `minLevel` field (optional - blank means "no known requirement," matching how Cannith's own level gating already worked before this). `handleCharLevelChange()`'s existing "lower the level, get warned what disappears" flow (previously Cannith-selections-only) now also scans Named/Custom categories against this - a category whose item's `minLevel` exceeds the new level gets added to the same confirmation prompt, and on confirm its `categoryMode` reverts to `"cannith"`. Non-destructive, same as the existing manual checkbox toggle (`handleCategoryModeToggle()`) already was: the named item's data is untouched in memory, so raising the level again (or manually re-checking Named/Custom) picks up right where it was.
  - *Library*: new `namedItem` table (`userId` + `itemName` unique per user, `itemData` JSON blob) and `GET`/`POST /api/named-items` (`server/src/routes/namedItems.ts`, upsert via `ON DUPLICATE KEY UPDATE` - "any per-user named item is just saved over the existing named item" per the design ask, no separate create/update path needed). Deliberately a **snapshot, not a live reference**: picking a name copies that library entry's data into the current build at that moment; nothing stays linked afterward, matching how `buildChecksum`/History/Rollback already treat a saved build as a point-in-time record. The `customItemName` input is now a `<input list>`/`<datalist>` combo box (free text or pick an existing name - zero custom dropdown JS needed) fed by a new `Save Named Item` button next to it. The library payload is the full item, not just its shape: augment slot colors AND their selected enchantments (`selections.positional['custom:'+category]`) AND selected inherent effects (`selections.inherent[category]['custom:'+category]`) all round-trip, not just the name/description that `customItems[category]` alone would capture. Verified with `test/harness/named-item.js` (full round-trip restore of augment/inherent selections; the minLevel-cascade confirm-and-revert) and, after deploying, a live end-to-end pass: real `POST`/`GET`/upsert against the deployed `/api/named-items` (confirmed upsert reuses the same row, doesn't duplicate), plus the harness re-run against the live `/api/catalog`+`/api/character-classes`+`/api/named-items` endpoints together. Part 3 (promoting a personal named item to a shared/global list) is intentionally not built - see `TO DO.md`'s Maybe list.
- Folded the Named Item Min Level field into the Name row (inline label+input, like Character Info's Name/Level fields sit side by side, but without their stacked label-above-input treatment - that costs more vertical height than a per-category row inside a whole table of them can afford) instead of its own separate table row.
- **Named Item editor revamp: dirty tracking, a relocated/relabeled Save button, an overwrite confirmation, a Save/Discard/Cancel guard on switching back to Cannith, and a proper "start a new item" combo box flow.**
  - *Dirty tracking*: `lastSavedNamedItemSnapshot[category]`/`isNamedItemDirty()`/`markNamedItemSaved()` - the same `lastSavedSnapshot`/`isDirty()`/`markSaved()` pattern the whole build already used, one level down: does this category's item differ from its own library entry, not from the last-saved build. Baselined (clean) on a fresh blank item, right after loading from the library, and right after a successful save; left dirty by default after opening a whole saved build unless the loaded item happens to match its library entry exactly (`syncNamedItemBaselineFromLibrary()`). A category being library-dirty needs no separate wiring to also dirty the whole build - `customItems` is already part of `computeContentSnapshot()`, so any edit here already flips the build's own `isDirty()` on its own; saving to the library deliberately does not un-dirty the build, since the build itself still needs its own Save.
  - *Button*: moved from next to the Name field down into the Inherent Effects row, stacked below its Edit/Done toggle (there's room, and both disappear together whenever the whole category collapses to nothing - see `renderEnchantmentOptions()`). Label and enabled state are driven by the dirty flag: `"Save Named Item"` and disabled when clean (nothing to do), `"Save"` and enabled when dirty.
  - *Overwrite confirmation*: `handleSaveNamedItem()` now confirms before overwriting an existing library entry (a real "lose data" moment if what's currently entered has fewer augments/effects than what's already saved there) - a brand new name saves silently, since there's nothing to lose.
  - *Cannith-switch guard*: unchecking Named/Custom on a category with a dirty item now asks first - reused the exact two-chained-`confirm()` pattern `confirmDiscardUnsavedChanges()` already established as this app's stand-in for a three-way Save/Discard/Cancel choice (see its comment: "consistent with every other dialog in this app being a native `confirm()` rather than a custom modal"), rather than building new modal UI for a second case of the same problem. Only triggers when dirty - an already-clean or never-touched item switches away with no prompt, matching the existing non-destructive toggle behavior.
  - *Setup gating*: a brand-new, still-empty Named/Custom item locks to just Name/Min Level - `namedItemNeedsSetup()` hides the augment-add control, the Inherent Effects row, and the description field until both are filled in, the same "nothing to do until the basics are set" idea as the whole page forcing every category collapsed until `charLevel` is valid. Never re-locks an item that already has real content, even if minLevel is later cleared - that would look like the app silently hid or lost work.
  - *Combo box "start a new item"*: typing a name that doesn't match the library now behaves differently depending on whether what's currently entered is dirty. If it's clean (freshly loaded, untouched, or the still-blank default) the rest of the item resets to blank under the new name - lets a user pick an existing item, then deliberately type a new name to start defining a different one, without dragging the old item's augments along by accident. If it's dirty (mid-edit), nothing resets - fixing a typo in the name mid-edit doesn't silently discard the augments/effects already entered.
  - Verified with a new `test/harness/named-item-editor.js` (28 checks - dirty transitions, the setup gate, both Save-button states, the overwrite confirm's accept/decline paths, and all three of the Cannith-switch guard's Save/Discard/Cancel branches) and the existing `named-item.js`/`example.js` re-run clean. Needed one small harness upgrade: `dom-stub.js`'s `confirm()` stub now accepts a function for `confirmReturns` (previously only a fixed true/false) so a test can script a sequence of different answers - e.g. "decline the Save prompt, then accept the Discard prompt" - the same "improve the harness itself when a real test needs more from it" approach as everything else in `test/harness/`.
  - A future, more ambitious Named Item Editor (typed/picked effects instead of just augment slots, optional magnitude, private-then-promotable-to-global effects) is logged in `TO DO.md`, not built - this batch is the groundwork, not that redesign.
- **Named Item editor follow-up round**, all from real usage feedback the same day:
  - Save button always reads "Save" now - only its enabled state changes with the dirty flag, matching how the main Save icon (`updateSaveDownloadEnabled()`) only ever toggles disabled/enabled, never its own label. Also dropped a bespoke, weaker `.saveNamedItemBtn:disabled` CSS rule that was fighting the app's real global `button:disabled` convention rather than using it - same fix applied to the new Delete button so both look consistently "really not clickable," not just dimmed.
  - Description moved out of its own row into the Inherent Effects box, with its own "Description" label (not merged into the "Inherent Effects" slot label) - it's meant to document inherent effects that defy capture with a single button, so it belongs with them. Follows the same collapsed flag as the rest of that row.
  - Fixed a real bug: `handleCustomItemMinLevel()` never called `renderEnchantmentOptions()`, so filling in Min Level (the second half of what unlocks augments/Inherent Effects/description - see `namedItemNeedsSetup()`) silently did nothing until some unrelated action forced a re-render. Every other field-change handler in this area already re-rendered; this one had just been missed.
  - Min Level is now capped at the character's own current level (not the general 1-36 range) - doesn't make sense to declare an item needs a level you haven't reached yet while you're the one defining it. The combo box is now also filtered to only offer items whose minLevel the current level actually allows, re-evaluated live as the level changes (see `renderNamedItemDatalist()`).
  - **Augment selections are no longer saved to the library at all** - only the augment slots themselves (their colors). Which specific enchantment fills a slot is a per-build choice, not part of what the named item itself IS; loading a Named Item now always starts its augment slots empty, the same as picking a fresh Cannith item would. Inherent effects, by contrast, are a real property of the item and still round-trip.
  - **Named items now belong to one specific category**, not just one user - a real DDO named item only fits one equipment slot, so "Tourney Armor" in Boots and "Tourney Armor" in Melee1 are unrelated items that happen to share a name, not a collision. Added a `category` column to `namedItem` (its unique key is now `userId+category+itemName`) - the table had no real user data yet, so this was a drop-and-recreate, not a migration. The single shared `<datalist>` became a separate one rendered inline per category (`renderNamedItemDatalist()`/`namedItemDatalistId()`), replacing `updateNamedItemDatalist()` and every scattered call to keep it in sync - the per-category version is naturally always correct, computed fresh on every render instead of needing to be told when to update.
  - **Fixed a real combo box bug**: once the Name field's value exactly matched a datalist option, some browsers (Chrome especially) stop offering any alternative suggestions at all - a well-known `<input list>` quirk. Standard workaround: clear the field on focus so every option shows again, restore it on blur only if the user left it empty without picking or typing anything (`handleCustomItemNameFocus()`/`handleCustomItemNameBlur()`, via `dataset.priorValue`).
  - **Switching to a different existing Named Item is now dirty-aware**: a clean current item switches immediately with no prompt at all ("switch easily"); a dirty one gets a Save/Discard/Cancel guard, initially built as two chained `confirm()`s (matching `confirmDiscardUnsavedChanges()`'s established pattern) before being replaced by a real modal the same day - see below. Replaces the old blanket "this will replace what's here, continue?" confirm, which fired even when there was nothing at risk (an already-saved, untouched item).
  - Verified with `test/harness/named-item-editor.js` rewritten around the new behavior (47 checks) plus `named-item.js` updated to match (augment slots restore, selections don't) - both re-run clean, along with `example.js`.
- **Replaced the chained-`confirm()` Save/Discard/Cancel guards with a real 3-button modal**, after Jeff reported the chained version (build a Named Item, start a second one unsaved, try switching back to the first) as confusing: clicking Cancel on "Save changes...?" silently opened a *second* native dialog to reach Discard, which read as a glitchy double-prompt rather than one clear three-way choice - he initially thought it was a bug. New `#saveDiscardCancel` modal (`ddocraft.html`) with three explicitly labeled buttons, driven by `confirmSaveDiscardCancel(message, onSave, onDiscard, onCancel)` in `ddocraft.js` - non-blocking (button clicks, not a blocking `confirm()`), reassigns `.onclick` on each call rather than `addEventListener` so reusing the one dialog never stacks duplicate callbacks. Swapped into both places that had the chained-confirm problem: the Named Item combo box switch guard (`handleCustomItemName`) and the Cannith-switch guard (`handleCategoryModeToggle`) - the latter hadn't been reported broken yet, but is the identical pattern, so got the same fix pre-emptively rather than waiting for the same bug report twice. Deliberately left `confirmDiscardUnsavedChanges()` (the whole-build save guard) on the old chained-`confirm()` pattern - not the one reported, and changing it wasn't asked for; flagged as the same latent risk in `TO DO.md` instead. `handleSaveNamedItem()`'s own overwrite-confirmation is unaffected - still a plain two-way native `confirm()`, since that's a real two-way choice, not a three-way one. `test/harness/dom-stub.js` gained one small fix to support this: `dispatchEvent()` now also invokes an assigned `on<type>` property handler (e.g. `el.onclick = fn`), not just `addEventListener` listeners, matching real DOM behavior - the modal's buttons use property assignment. `named-item-editor.js` updated to drive the modal directly (`clickModal()`) instead of scripting confirm sequences.
