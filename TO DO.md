# To Do

1. Better visual treatment for collapsed items (standard chevrons?).
2. **Real accounts for `characterBuild.userId`**: the DB-backed save/open/history/rollback/delete mechanism itself is built and working (see Done.md) - what's left is swapping `getUserId()`'s hardcoded `1` for a real authenticated session's userId. Requires GateIron.com's shared account system (site-wide login via Auth.js, MySQL-backed sessions, Google/GitHub OAuth) to exist first - that's being designed/built as a GateIron.com-level effort, not scoped to this repo.
3. Let effect descriptions use `effectBonusByLevel` (real per-level numeric bonuses) instead of the static `effectDescription` string, now that `/api/catalog` is live and the plumbing exists to join it in.
4. **Checksum-based duplicate detection at save time** - added 2026-07-30 alongside multi-classing. If a new save's `buildChecksum` matches another *different-named* build already in that same user's stable (not just the same-name overwrite case `POST /` already handles), offer to treat it as the same build (replace/link it) instead of silently creating an independent duplicate. No design yet - worth deciding what "offer to replace" actually looks like (which one wins? does it rename? does History still make sense across two different names?) before building.
   - Related open question, not yet decided: `computeBuildChecksum()` currently includes `charLevel` alongside the equipment/effect selections (see `server/src/routes/characterBuilds.ts`) - two otherwise-identical builds at different levels get different checksums today. Whether that's right depends on what the checksum is *for*: capturing "same set of items/effects chosen" (level-independent) vs. "same set of items/effects *and the same legal options at the time*" (level matters, since level gates which selections are even choosable). Revisit when this item gets designed, since it directly affects how "duplicate" gets detected.
5. **A real Named Item Editor** - added 2026-07-30, alongside the min-level/library/dirty-tracking work (see Done.md) that's a step toward this, not the thing itself. Jeff's framing: the current custom-item UI (augment slots + a flat alphabetical ~1500-row Inherent Effects picker) is close, but has too much surface area immediately visible. Direction, not yet designed in detail:
   - Dramatically reduce what's shown by default - closer to how augment slots work today (add one at a time via "+ Add Augment") than the current always-fully-listed Inherent Effects picker.
   - Adding an *effect* to a Named Item (not just picking from the ~200 existing catalog effects) should work like the augment-add flow: pick an existing effect via a search/filter-friendly mechanism (200 is too many for a flat list to stay friendly), or type a brand new effect name that doesn't exist in the catalog yet.
   - Each added effect should have an optional magnitude field alongside it - the catalog's `effectBonusByLevel` join (see item 3 above) is the natural source for what a magnitude even means for a given effect, so this probably wants that item done first or alongside.
   - A user-typed new effect (not from the catalog) should default to private/personal-only, the same "yours until someone flips a switch" shape as the Named Item library's own deferred Part 3 (promoting a personal named item to global - see the Maybe list) - i.e. this needs its own analogous private-effect-table-with-a-promotion-path design, not a one-off.
   - No schema/UI design done yet - this is a direction to build toward, not a scoped task.
6. **Replace the remaining native `confirm()`/`alert()` dialogs with the styled modal** - Jeff liked the new `#saveDiscardCancel` dialog (added 2026-07-30, see Done.md) enough to ask for the same treatment everywhere, not just the two Save/Discard/Cancel guards it currently covers. Full inventory of what's left, all still plain browser popups:
   - Three-way (chained `confirm()`s, same fixable pattern `confirmSaveDiscardCancel()` already replaced elsewhere): `confirmDiscardUnsavedChanges()` (leave with unsaved build changes) - already flagged on its own further down, subsumed by this item now.
   - Two-way yes/no (`handleDeleteNamedItem`, `handleSaveNamedItem`'s overwrite check, `confirmRevert()`, `handleSaveOverwriteConfirmation()`, `handleDeleteBuild()`, `handleRollback()`, `handleCharLevelChange()`'s level-decrease cascade) - would want a simpler 2-button sibling of the same modal (e.g. a verb + Cancel, no separate Discard button), not just reusing the 3-button one with a button hidden.
   - Plain notifications, no choice at all (every `alert(...)` call, mostly `.catch(err => alert(...))` failure messages) - lowest priority, but the same "styled instead of a jarring native popup" logic applies; a toast/inline-status treatment might fit these better than a modal, worth deciding when this gets designed (also see the "Save's success feedback" Known Issue below, which wants something similar).
   - No design done yet on the exact shared component shape (one flexible modal with configurable buttons? separate 2-way/3-way variants?) - scope this properly before building rather than duplicating `confirmSaveDiscardCancel()`'s HTML/CSS/JS per call site.

## Known Issues

*(tracked, not urgent, not necessarily wrong — revisit when there's reason to)*

- The 6 remaining `enchantment` rows excluded as `dataStatus='questionable'` (Resist/Insightful-Resist Light/Negative/Poison) are believed correct but never independently re-verified. (The 6 Insightful Ability rows were reversed out of this list 2026-07-28 - see Done.md.)
- Flametouched Iron, Crystal, and Transmuting (new weapon materials added 2026-07-28) have placeholder/TBD `enchAugmentMinLevel` values - real levels not yet confirmed, same caveat as the other TEMP/TEST DATA rows.
- `WEAPON_CATEGORIES` (Melee1/Melee2/Ranged) in `ddocraft.js` is still a hardcoded guess, used only for the custom/named-item augment-color picker (which combo colors - Orange/Purple vs Green - to offer, since custom items have no DB backing at all). Distinct from Cannith rendering, which now gets its real per-slot color eligibility straight from `/api/catalog` (see Done.md, 2026-07-30) - including a confirmed-real DDO rule that a weapon/orb always has exactly one Red-eligible augment slot, never both non-Red colors together. Whether that same rule should extend to the custom-item color picker (and whether Orb belongs in `WEAPON_CATEGORIES` alongside the three Melee/Ranged categories, given it follows a similar-but-not-identical pattern - no Red at all, just an asymmetric Blue/Yellow split) is a real open question, not assumed either way - don't guess further at this without Jeff confirming, since the adjacent Cannith-rendering assumption was already wrong once tonight.
- The recovered enchantment master table was built from Cannith-crafting data, so it may be missing (effect, exotic-bonus-type) combinations that only exist on named items — e.g. an effect that normally stacks via an unusual bonus type when found on a named item might not have that bonus-type variant in the table. No fix proposed; unclear how big a problem this is in practice.
- `effectEquivalencyGroup`/`effectEquivalencyMember` (`db/ddocraft_schema.sql`) — the group+member+qualityScore design isn't clearly right, but no better alternative has surfaced either. Revisit once real supersedence data forces the question.
- `effectBonusByLevel` has no die-type column. Five effects (Bashing, all 24 Bane creature types, all 16 Damage(X) elemental/alignment types via both delivery types, Shield Spikes, Vampirism) are dice-based — the stored bonus is the die *count* only (die size: d6 for all of them except Bane which is d10, and Vampirism which is d2). Matters if the bonus value ever needs to drive real damage math instead of just display/comparison.
- Fear Immunity's `minLevelAugment` is 100 (the "not augmentable" sentinel used on ~181 rows), but the recovered comparison chart shows it augment-available at level 8 — same as its sibling Blindness Immunity, which is correctly set. Looks like a gap in the original recovered data; not corrected yet.
- The dev VM (`codehawkins`, Ubuntu 18.04) has a glibc too old for any current Node.js LTS — even the last Node 18.x build requires a newer glibc than it has. Running Node 16 there instead, which is already past its own end-of-life. Fine for dev/QA; don't let it become the assumed production runtime without a real decision.
- `characterClass` (added 2026-07-29) is seeded with the 15 base classes only. DDO's Iconic Heroes (distinct class variants tied to a specific race/backstory, e.g. Bladeforged) are NOT seeded yet - Jeff flagged that the current roster needs confirming before populating `parentClassId`-linked rows, since guessing at DDO lore here risks baking wrong data into the DB. Confirm the current list with Jeff (or research it) before adding these.
- History dialog has some other screwiness Jeff noticed 2026-07-30, similar in nature to the Open dialog issues fixed that day (see Done.md) - not yet characterized further. Revisit and characterize before fixing.
- Save's success feedback needs a better answer than "no feedback at all" - removing the old success `alert()` (see Done.md, unsaved-changes tracking) was the right call since it was firing on every click including no-op resaves, but a silent Save button going disabled is easy to miss entirely, especially if something actually goes wrong client-side before the request is even sent (as happened 2026-07-30 - see Done.md, the CHARACTER_BUILD_API_BASE crash). Some lighter-weight confirmation (a toast, a brief inline status) is probably right; not designed yet.
- Multi-classing (added 2026-07-30, see Done.md) has no combination validation - a user can currently pick any 3 classes regardless of DDO's real restrictions (e.g. Paladin requires Lawful alignment; some classes can't be Lawful at all, making certain combos impossible in-game). Deliberately not enforced yet - Jeff flagged this as a known gap to handle later, not a bug to fix now.
- `confirmDiscardUnsavedChanges()` (the whole-build "unsaved changes, leave anyway?" guard) still uses the old two-chained-`confirm()` trick Jeff found confusing elsewhere (Cancel on the first dialog silently opens a second one to reach Discard) - see item 6 in the numbered TO DO list above, which now covers this along with every other remaining native `confirm()`/`alert()` in the app.
- Named Item library Delete (added 2026-07-30, see Done.md) is a real hard delete with no undo, same honesty as build deletion (see the Maybe item below) - the confirm text says so, but there's no recovery path if it turns out to be the wrong call.

## Maybe

*(explicitly deferred - no commitment to build, just not forgotten)*

- Class-based filtering/gating, now that multi-classing (2026-07-30, see Done.md) gives the app real per-build class data to key off - e.g. only Artificers can have Rune Arms, only spellcasters benefit from Orbs. Could also filter the whole page by class to reduce complexity for users who don't need to see irrelevant categories. No design yet; depends on the class-conflict validation (see Known Issues) landing first, or at least not being contradicted by it.
- Shared multi-party editing (two people collaboratively viewing/editing the *same* character build in real time) — deferred 2026-07-29. The "Open a build from someone else's share link" design (see Done.md once built) is single-owner: opening a shared build forks it into a new, independent build owned by the opener, never a live-shared/co-edited row. Real multi-party editing is a materially bigger feature (concurrency, conflict resolution, presence) and has no design yet.
- Undo for deleting a build's last active version — deferred 2026-07-29. Delete (Open Build dialog) is a soft delete, so the row survives in the DB, but there's currently no UI path back to it: History is only reachable from an active row under that name, and deleting the last one removes that path entirely. A real fix means a general "browse fully-deleted/orphaned builds" recovery screen - close to the broader "Recover Deleted" design considered and set aside in favor of the simpler per-build Rollback/History feature (see Done.md). Not building now; the Delete confirmation is honest about there being no undo today.
- An optional prepopulated dropdown of *popular* named/custom items shared across all users, seeded once real usage data exists to mine (e.g. several independent users entering the same effect set for the same item name). Distinct from the per-user Named Item library added 2026-07-30 (see Done.md), which is private to each user by design - this would be a separate, opt-in-to-see, cross-user popularity signal layered on top.
- Named Item library Part 3 (added 2026-07-30, see Done.md - Parts 1/2 are built): promote one of a user's own named items to a shared/global list everyone's combo box can find, not just its owner's. The combo box's datalist would need to merge global + personal (alphabetized, per the original design ask) instead of just personal. No design yet for who can promote, whether a promoted item can still be edited by its original owner, or how a name collision with another user's global item gets resolved.
- The Character Info section's brown/light-orange table styling is now kept on the main page and (with matching dark chrome) in every dialog, including Help/About as of 2026-07-30 (see Done.md) - not just a preview anymore. Individual controls (buttons, inputs) still don't fully match the aesthetic though - worth a proper design pass rather than piecemeal color swaps.
- About's copyright line changed from "2021" to "2026" (a single year, not a range) on 2026-07-30 per Jeff's request - see Done.md. Quick research at the time found no legal basis for a range "weakening" a claim (both formats satisfy 17 U.S.C. §401(b); the whole notice isn't even legally required post-Berne), so this was a stylistic call, not a legal fix. Worth knowing if it comes up again: a year range (first-published year through most-recent-update year) is the more commonly recommended format for a continuously-updated site/app specifically, which is what this is - the single year traded a bit of that accuracy for simplicity.
- About is explicitly a placeholder pending a real licensing discussion (2026-07-30, see Done.md) - it currently has no license text at all (removed along with the old GPL 3.0 mention). Whatever GateIron.com lands on for licensing needs to come back here.
- Real usage/analytics data — deferred 2026-07-30, noted when the PHP-based visitor counter was removed (see Done.md) rather than rebuilt. Jeff wants better data than a single running total once the DB-backed system is further along (e.g. real per-build/per-session activity queryable from MariaDB), not another file-based counter. No design yet.
