# Local secrets - never commit. See db/ for what these hold. (.env.example is the safe template,
# intentionally still tracked.)
.env
.env.local

# Local-only test harness, created/deleted during development sessions.
ddocraft_preview.html

# Node.js server build output/dependencies - installed/built on demand, not tracked.
server/node_modules/
server/dist/
